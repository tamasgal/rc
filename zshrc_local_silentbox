eval "$(micromamba shell hook --shell zsh)"

# # >>> conda initialize >>>
# # !! Contents within this block are managed by 'conda init' !!
# __conda_setup="$('/Users/tamasgal/radioconda/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
# if [ $? -eq 0 ]; then
#     eval "$__conda_setup"
# else
#     if [ -f "/Users/tamasgal/radioconda/etc/profile.d/conda.sh" ]; then
#         . "/Users/tamasgal/radioconda/etc/profile.d/conda.sh"
#     else
#         export PATH="/Users/tamasgal/radioconda/bin:$PATH"
#     fi
# fi
# unset __conda_setup
# # <<< conda initialize <<<

PATH="/opt/homebrew/opt/grep/libexec/gnubin:$PATH"

. ~/.zshrc_secret

# >>> juliaup initialize >>>

# !! Contents within this block are managed by juliaup !!

path=('/Users/tamasgal/.julia/juliaup/bin' $path)
export PATH

# <<< juliaup initialize <<<
#
export HPSS_LOCAL_STORE=/Volumes/Ultraspeed/HPSS_Lyon
export PATH=$PATH:$HOME/Dev/jpp/build-basic/stage/bin
#pushd /opt/homebrew >/dev/null; . bin/thisroot.sh; popd >/dev/null
